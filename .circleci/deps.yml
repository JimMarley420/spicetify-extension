version: 2.1

jobs:
  update-deno-deps:
    docker:
      - image: denoland/deno:latest

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: |
            apt-get update && apt-get install -y git curl jq

      - run:
          name: Detect outdated dependencies
          command: |
            deno outdated > outdated.txt || true
            cat outdated.txt

      - run:
          name: Stop if nothing to update
          command: |
            if [ ! -s outdated.txt ]; then
              echo "No updates"
              circleci step halt
            fi

      - run:
          name: Configure git
          command: |
            git config user.email "ci@circleci.com"
            git config user.name "circleci-bot"

      - run:
          name: Create update branch
          command: |
            BRANCH="deps/auto-update-$(date +%Y%m%d%H%M%S)"
            echo "export UPDATE_BRANCH=$BRANCH" >> $BASH_ENV
            git checkout -b $BRANCH

      - run:
          name: Update dependencies
          command: |
            deno outdated --update

      - run:
          name: Commit changes
          command: |
            git add deno.json deno.lock || true
            git commit -m "chore(deps): update deno dependencies"

      - run:
          name: Push branch
          command: |
            git push origin $UPDATE_BRANCH

      - run:
          name: Create Pull Request
          command: |
            PR_RESPONSE=$(curl -s -X POST \
              -H "Authorization: token $GITHUB_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              https://api.github.com/repos/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME/pulls \
              -d "{
                \"title\": \"chore(deps): update deno dependencies\",
                \"head\": \"$UPDATE_BRANCH\",
                \"base\": \"main\",
                \"body\": \"Automated dependency update via CircleCI.\"
              }")

            echo $PR_RESPONSE | jq .

      - run:
          name: Create Issue
          command: |
            BODY=$(cat outdated.txt | sed 's/"/\\"/g')

            curl -X POST \
              -H "Authorization: token $GITHUB_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              https://api.github.com/repos/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME/issues \
              -d "{
                \"title\": \"🔄 Deno dependencies update available\",
                \"body\": \"\`\`\`\n$BODY\n\`\`\`\"
              }"

workflows:
  deps:
    jobs:
      - update-deno-deps
