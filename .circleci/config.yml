version: 2.1

jobs:
  build:
    docker:
      - image: denoland/deno:latest

    steps:
      - checkout

      - run:
          name: Run build task
          command: deno task build

workflows:
  build-workflow:
    jobs:
      - build