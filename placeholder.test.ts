// Placeholder test to enable CI test job.
// This file exists only to prevent "No Test Modules Found" errors in CI.
// Contributors should replace this with real tests as the codebase grows.

import { assertEquals } from "@std/assert";

Deno.test("placeholder test", () => {
  assertEquals(1 + 1, 2);
});